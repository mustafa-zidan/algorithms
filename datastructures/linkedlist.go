package main

type LinkedList struct {
	length   int
	Previous LinkedList
	Val      interface{}
	Next     LinkedList
}

func Add(item interface{}) {
}

func Remove(i index) {}

func Get(i index) interface{} { return nil }
